from config import *

# def get_timesales (symbol, interval='1min', start_date=False, end_date=False):
	
# 	r = requests.get(
# 		url 	= '{}/{}'.format(SANDBOX_URL, QUOTES_TIMESALES_ENDPOINT),
# 		params 	= {
# 			'symbol' : symbol,
# 			'interval' : interval,
# 			''
# 		})